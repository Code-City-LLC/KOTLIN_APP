# Gradle
.gradle/
build/
app/build/

# Local config (SDK path, signing secrets)
local.properties
keystore.properties
*.keystore
*.jks
!debug.keystore

# IDE
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# Kotlin incremental-compile session files (build noise)
.kotlin/

# Google services config is provisioned per-environment; never commit real one blindly
# (tracked intentionally when Kemar provides it)

# Play Developer API service-account keys — credentials, never commit
tools/*.json
play-service-account*.json
__pycache__/
